from django.contrib import admin

# SEO app currently has no models registered.
# Once models are added, register them here with full admin customization.

# Example template:
# from .models import SomeSeoModel
#
# @admin.register(SomeSeoModel)
# class SomeSeoModelAdmin(admin.ModelAdmin):
#     list_display = ('id', '__str__')
#     list_filter = ('created_at',)
#     search_fields = ('name',)
